# 第一阶段（第1-4周）阶段报告 — 可运行的数据库原型 v0.1

## 一、设计思路

### 1.1 经营场景与业务流程

本项目以**校园小卖部**为真实经营场景，设计并实现一个完整的关系型数据库管理系统。选择该场景的理由：小组成员均为在校学生，对小卖部运营流程熟悉；场景复杂度适中，天然包含商品、库存、订单、订单明细、会员、员工六类核心对象，覆盖关系数据库设计的主要知识点；同时具备良好的可扩展性，可在后续阶段逐步增加促销、采购、盘点等功能。

**核心业务流程**如下：

```
顾客进店 → 浏览商品 → 挑选商品 → 店员结账 → 生成订单 → 扣减库存
     ↓
  会员积分（如为会员）
     ↓
  交接班/销售统计
     ↓
  库存预警 → 补货采购
```

除核心交易流程外，还梳理了以下扩展流程：
- **会员注册流程**：顾客提供手机号注册会员，系统记录会员信息
- **库存监控与补货流程**：库存低于阈值时触发预警，店长审批补货
- **库存盘点流程**：定期盘点实物库存与系统库存，调整差异
- **员工交接班流程**：清点收银现金、核对销售记录
- **退货退款流程**：顾客申请退货，店员/店长审批后退款并恢复库存

### 1.2 数据边界：哪些信息进入数据库，哪些暂不进入

#### 进入数据库的信息（六类核心对象，共49个字段）

| 对象 | 表名 | 核心字段 | 进库理由 |
|---|---|---|---|
| 商品 | `product` | 编号、名称、分类、售价、进价、单位、供应商、生产日期、保质期、状态 | 交易核心对象，销售和库存管理的基础 |
| 库存 | `inventory` | 记录编号、商品编号、数量、货架位置、最低阈值、最后更新时间、更新人 | 库存管理必需，支撑库存预警和销售扣减 |
| 订单 | `orders` | 订单号、下单时间、总金额、支付方式、订单状态、会员编号、收银员编号、备注 | 交易记录核心，支撑销售统计和财务核对 |
| 订单明细 | `order_item` | 明细编号、订单号、商品号、数量、成交单价、小计 | 记录订单中每个商品的明细，支撑销售分析 |
| 会员 | `member` | 会员编号、姓名、手机号、注册时间、积分、等级、累计消费、最后消费时间、状态 | 会员管理必需，支撑积分和等级权益 |
| 员工 | `employee` | 员工编号、姓名、岗位、登录账号、密码哈希、手机号、入职时间、状态、最后登录时间 | 权限管理必需，支撑操作审计和收银员关联 |

#### 暂不进入数据库的信息及理由

| 信息类别 | 暂不进库理由 | 后续扩展可能性 |
|---|---|---|
| 供应商信息 | 第一阶段聚焦销售闭环，采购流程暂未纳入 | 第二阶段可增加 supplier 表，关联商品的供应商字段 |
| 财务报表（收入/成本/利润核算） | 可通过订单和商品数据实时计算，无需冗余存储 | 后续可增加财务汇总表用于性能优化 |
| 促销活动（优惠券/折扣） | 第一阶段所有商品按标价销售，促销规则复杂 | 后续可增加 promotion 表和订单折扣字段 |
| 员工考勤（排班/打卡） | 第一阶段只记录员工基本信息和销售业绩，考勤属人事模块 | 后续可增加 attendance 表 |
| 非会员顾客个人信息 | 隐私保护原则，非会员交易不记录任何个人身份信息 | 不计划扩展，始终保护顾客隐私 |
| 商品图片 | 第一阶段为纯文字描述，图片存储增加复杂度 | 后续可增加 image_url 字段 |
| 员工薪资/身份证号 | 隐私敏感，且第一阶段最小闭环不需要 | 后续人事模块可加密存储 |

**数据边界划分原则**：交易核心原则（直接参与交易的数据必须进库）、持久化原则（需要长期保存的数据进库）、可查询原则（需要统计分析的数据进库）、最小化原则（非核心数据暂不进库）、隐私保护原则（敏感个人信息尽量不进库或加密存储）。

---

## 二、实验过程

### 2.1 表结构设计

本系统共设计6张表，49个字段。表间关系如下：

- `product` 1:1 `inventory`（一个商品对应一条库存记录）
- `product` 1:N `order_item`（一个商品可出现在多个订单明细中）
- `orders` 1:N `order_item`（一个订单包含多个明细）
- `member` 1:N `orders`（一个会员可有多个订单，非会员订单 member_id 为空）
- `employee` 1:N `orders`（一个收银员可处理多个订单）
- `employee` 1:N `inventory`（一个员工可更新多条库存记录）

#### 主码（Primary Key）设置及理由

| 表名 | 主码 | 类型 | 选择理由 |
|---|---|---|---|
| product | `product_id` | VARCHAR(10) | 业务编码，格式 P+4位序号，全局唯一标识商品，避免因商品名称重复或改名导致主键变化 |
| member | `member_id` | INT 自增 | 代理键，全局唯一标识会员；手机号虽唯一但可能更换，不宜作主码 |
| employee | `employee_id` | INT 自增 | 代理键，全局唯一标识员工；登录账号虽唯一但可能调整 |
| inventory | `inventory_id` | INT 自增 | 代理键，全局唯一标识库存记录；product_id 虽在1:1关系下唯一，但独立主键便于未来扩展多仓库 |
| orders | `order_id` | VARCHAR(20) | 业务键，格式 ORD+8位日期+4位序号，本身具有业务含义且全局唯一，适合直接作主码 |
| order_item | `item_id` | INT 自增 | 代理键，全局唯一标识明细行；(order_id, product_id) 虽可作复合主键，但独立主键简化明细行的引用和操作 |

**主码选择原则**：优先选择稳定、唯一、简洁的属性。业务键（如 order_id）在具有明确业务含义且稳定时直接使用；其他表使用代理键（自增ID或编号），避免因业务属性变化影响主键。

#### 候选码（Candidate Key）分析

| 表名 | 候选码 | 说明 | 是否选为主码 |
|---|---|---|---|
| product | `product_id` | 系统生成的商品编号，唯一且非空 | ✅ 选为主码 |
| member | `member_id` | 自增会员ID，唯一且非空 | ✅ 选为主码 |
| member | `phone` | 手机号，业务上唯一且非空 | ❌ 未选（可能更换），通过 UNIQUE 约束保证唯一性 |
| employee | `employee_id` | 自增员工ID，唯一且非空 | ✅ 选为主码 |
| employee | `login_account` | 登录账号，业务上唯一且非空 | ❌ 未选（可能调整），通过 UNIQUE 约束保证 |
| inventory | `inventory_id` | 自增库存ID，唯一且非空 | ✅ 选为主码 |
| inventory | `product_id` | 商品ID，1:1关系下唯一 | ❌ 未选（未来多仓库扩展后不唯一），通过 UNIQUE 约束保证 |
| orders | `order_id` | 订单号，业务唯一且非空 | ✅ 选为主码 |
| order_item | `item_id` | 自增明细ID，唯一且非空 | ✅ 选为主码 |
| order_item | `(order_id, product_id)` | 订单号+商品号复合，同一订单中同一商品唯一 | ❌ 未选（复合键操作复杂），通过 UNIQUE 约束保证 |

**候选码与主码的关系**：每个表至少有一个候选码被选为主码，其他候选码通过 UNIQUE 约束强制其唯一性。例如 member 表的 phone、employee 表的 login_account、inventory 表的 product_id、order_item 表的 (order_id, product_id) 均为候选码但未选为主码，通过 UNIQUE 约束保证唯一性。

#### 外码（Foreign Key）设置及理由

| 表名 | 外码字段 | 引用表.字段 | 设置理由 |
|---|---|---|---|
| inventory | `product_id` | product.product_id | 库存记录必须对应一个存在的商品，1:1关系 |
| inventory | `last_updated_by` | employee.employee_id | 库存更新操作人必须是存在的员工 |
| orders | `member_id` | member.member_id | 会员订单必须对应存在的会员；非会员订单为 NULL（允许空） |
| orders | `cashier_id` | employee.employee_id | 订单收银员必须是存在的员工 |
| order_item | `order_id` | orders.order_id | 订单明细必须属于一个存在的订单 |
| order_item | `product_id` | product.product_id | 订单明细中的商品必须是存在的商品 |

**外码设置原则**：所有表间引用关系均通过外码约束保证参照完整性。允许空的外码（如 orders.member_id）用于表示可选关系（非会员订单）。外码字段均建立索引（MySQL 自动为外码创建索引），保证连接查询性能。

### 2.2 完整性约束设计与验证

系统实现了6类完整性约束，共计：6个主码约束、6个外键约束、4个唯一约束、24个CHECK约束、若干DEFAULT和NOT NULL约束。

#### CHECK 约束示例

| 约束名 | 约束内容 | 业务含义 |
|---|---|---|
| chk_product_sale_price | `sale_price > 0` | 商品售价必须大于0 |
| chk_product_price_relation | `purchase_price <= sale_price` | 进价不高于售价，保证毛利为正 |
| chk_product_category | `category IN ('饮料','食品','文具','日用品','其他')` | 商品分类只能是预设枚举值 |
| chk_member_phone_format | `phone REGEXP '^[0-9]{11}$'` | 手机号必须是11位数字 |
| chk_order_item_subtotal | `subtotal = quantity * unit_price` | 明细小计必须等于数量×单价，保证计算一致性 |
| chk_orders_total_amount | `total_amount >= 0` | 订单总金额不能为负 |

#### 约束验证正反例

**正例（合法数据，插入成功）**：

```sql
-- 正例：商品售价大于0，进价小于售价
INSERT INTO product (product_id, product_name, category, sale_price, purchase_price, unit, status)
VALUES ('P0011', '旺仔牛奶', '食品', 3.50, 2.00, '盒', '在售');
-- 结果：Query OK, 1 row affected

-- 正例：手机号11位数字
INSERT INTO member (member_id, member_name, phone, member_level, member_status)
VALUES (6, '测试会员', '13800138000', '普通', '正常');
-- 结果：Query OK, 1 row affected
```

**反例（非法数据，被约束拒绝）**：

```sql
-- 反例1：商品售价为负数，违反 CHECK 约束
INSERT INTO product (product_id, product_name, category, sale_price, purchase_price, unit, status)
VALUES ('PTEST', '测试商品', '食品', -5.00, 3.00, '个', '在售');
-- 结果：ERROR 3819 (HY000): Check constraint 'chk_product_price_relation' is violated.

-- 反例2：手机号格式错误，违反 CHECK 约束
INSERT INTO member (member_id, member_name, phone, member_level, member_status)
VALUES (7, '测试会员', '12345', '普通', '正常');
-- 结果：ERROR 3819 (HY000): Check constraint 'chk_member_phone_format' is violated.

-- 反例3：订单引用不存在的会员，违反外键约束
INSERT INTO orders (order_id, member_id, cashier_id, order_time, total_amount, payment_method, order_status)
VALUES ('ORDTEST', 9999, 1, '2026-09-16 12:00:00', 10.00, '现金', '已完成');
-- 结果：ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails
```

### 2.3 角色与权限划分

基于第一周的角色职能分析，创建4个数据库角色，遵循**最小权限原则**：

| 角色 | 权限范围 | 限制 |
|---|---|---|
| `store_manager`（店长） | 全部6张表的 SELECT/INSERT/UPDATE/DELETE + 所有视图查询 | 无（系统最高权限） |
| `cashier`（店员） | 商品SELECT、库存SELECT/UPDATE、订单SELECT/INSERT、明细SELECT/INSERT、会员SELECT + 3个视图 | 不能删除数据、不能管理员工、不能修改会员等级、不能查看员工表 |
| `member`（会员） | 商品SELECT、订单SELECT、明细SELECT + 1个视图 | 只读，不能查看其他会员信息，不能修改数据 |
| `guest`（顾客） | 仅商品SELECT | 不能查看订单、会员、员工、库存 |

#### 权限验证正反例

**正例（正常权限，操作成功）**：

```sql
-- 用店员账号登录
mysql -u cashier_test -pCashier@123

-- 正例：店员可以查看商品信息
SELECT product_id, product_name, sale_price FROM retail_store.product LIMIT 3;
-- 结果：返回3条商品记录，操作成功

-- 正例：店员可以创建订单
INSERT INTO retail_store.orders (order_id, order_time, total_amount, payment_method, order_status, cashier_id)
VALUES ('ORD202609230001', '2026-09-23 10:00:00', 10.00, '现金', '已完成', 2);
-- 结果：Query OK, 1 row affected
```

**反例（越权操作，被拒绝）**：

```sql
-- 反例1：店员尝试查看员工表（越权）
SELECT * FROM retail_store.employee;
-- 结果：ERROR 1142 (42000): SELECT command denied to user 'cashier_test'@'localhost' for table 'employee'

-- 反例2：店员尝试删除商品（越权）
DELETE FROM retail_store.product WHERE product_id = 'P0001';
-- 结果：ERROR 1142 (42000): DELETE command denied to user 'cashier_test'@'localhost' for table 'product'

-- 反例3：顾客尝试查看订单（越权）
mysql -u guest_test -pGuest@123
SELECT * FROM retail_store.orders;
-- 结果：ERROR 1142 (42000): SELECT command denied to user 'guest_test'@'localhost' for table 'orders'
```

### 2.4 查询与视图

围绕业务问题设计了16个多表查询，覆盖 INNER JOIN、LEFT JOIN、GROUP BY/HAVING、NOT IN 子查询、关联子查询、FROM 子查询等类型。创建了7个统计视图：订单详情视图、商品销售统计视图、库存状态视图、会员消费统计视图、员工销售业绩视图、每日销售汇总视图、分类销售统计视图。

**关键查询示例**——订单与会员的连接必须使用 LEFT JOIN，因为非会员订单的 member_id 为 NULL，使用 INNER JOIN 会丢失这些订单：

```sql
SELECT o.order_id, m.member_name, o.total_amount
FROM orders o
LEFT JOIN member m ON o.member_id = m.member_id
ORDER BY o.order_time;
-- 结果：包含10笔订单，其中非会员订单的 member_name 显示为 NULL
```

### 2.5 本地验证

所有脚本均在本地 MySQL 8.0.28 环境中实际执行验证：
- 建库、建表、插数脚本从空数据库完整执行通过
- CRUD 操作验证通过，事务处理正确
- 16个查询全部返回正确结果
- 7个视图创建成功，验证查询结果正确
- 6类约束正反例验证通过（正例成功，反例被拒绝）
- 4个角色权限验证通过（4个越权操作被拒绝，4个正常操作成功）

---

## 三、实验总结

### 3.1 完成情况

| 任务项 | 完成状态 | 说明 |
|---|---|---|
| 需求分析与业务建模 | ✅ | 6个业务流程、4类角色、数据边界清单 |
| 关系模式设计 | ✅ | 6张表、49字段、6主码、4候选码(UNIQUE)、6外码 |
| 建库建表SQL | ✅ | 可从空库完整复现，含24个CHECK约束 |
| 样例数据 | ✅ | 58条数据，表间关联正确 |
| CRUD操作 | ✅ | 商品/库存/订单三张表完整增删改查，含事务 |
| 多表查询 | ✅ | 16个查询，覆盖连接/聚合/子查询 |
| 统计视图 | ✅ | 7个视图，均附验证查询 |
| 完整性约束 | ✅ | 6类约束，正反例验证通过 |
| 角色权限 | ✅ | 4个角色，最小权限原则，越权测试通过 |
| 索引优化 | ✅ | 14个辅助索引 |
| 可复现性 | ✅ | 从空库依次执行全部脚本可复现 |
| 执行结果证据 | ✅ | 10个文本结果 + 7张操作截图 |

### 3.2 关键发现

1. **INNER JOIN vs LEFT JOIN 的选择至关重要**：订单与会员的连接必须使用 LEFT JOIN，因为非会员订单的 member_id 为 NULL，使用 INNER JOIN 会丢失这些订单。AI 最初建议使用 INNER JOIN，经人工识别后修正为 LEFT JOIN。

2. **完整性约束必须在数据库层面真正生效**：所有 CHECK 约束、外键约束、唯一约束都在 MySQL 8.0 中实际验证，反例操作确实被拒绝（ERROR 3819/1452/1062），而不是只在报告中写一条业务规则。

3. **角色权限遵循最小权限原则**：店员不能查看员工表、不能删除数据；顾客只能查看商品；会员只能查看自己相关的数据。AI 最初倾向于授予较宽泛的权限，经人工按照最小权限原则收紧。

4. **反例测试的脚本设计需要特殊处理**：反例 INSERT 会导致 MySQL 报错并中断整个脚本执行，因此采用注释方式呈现反例，正例实际执行，既保证脚本可完整运行，又保留了反例验证能力。

5. **业务键与代理键的选择需要权衡**：订单号（order_id）有业务含义且稳定，适合作为业务键直接作主码；而会员、员工等表使用自增代理键，避免手机号、登录账号等业务属性变化影响主键。

### 3.3 遇到的问题与解决

| 问题 | 原因 | 解决方案 |
|---|---|---|
| 建表顺序错误导致外键创建失败 | 引用表尚未创建 | 严格按外键依赖排序：product→member→employee→inventory→orders→order_item |
| 订单总金额与明细合计不一致 | AI 生成样例数据时计算错误 | 人工逐一复算每个订单的 total_amount，确保等于明细 subtotal 之和 |
| order_id 长度不够 | 订单号格式 ORD+8位日期+4位序号=15字符，原定义 VARCHAR(14) | 改为 VARCHAR(20)，并同步更新第二周设计文档 |
| product.unit 的 CHECK 约束覆盖不全 | 样例数据使用了桶/支/罐，约束只允许瓶/包/个/袋/盒/斤/其他 | 扩展约束枚举值，增加桶/支/罐 |
| 重复执行脚本时 DROP TABLE 因外键失败 | 外键约束导致删除顺序不对 | 在脚本开头添加 SET FOREIGN_KEY_CHECKS=0，结尾恢复为1 |
| 视图查询 information_schema.VIEWS 报错 | MySQL 8.0 无 CREATE_TIME 列 | 改用 IS_UPDATABLE 和 DEFINER 列 |
| role.sql 语法错误 | SET DEFAULT ROLE 用了 FOR 而非 TO | 改为 MySQL 8.0 正确语法 TO |

### 3.4 后续优化方向

1. **存储过程**：将订单创建（主表+明细+库存扣减）封装为存储过程，保证原子性
2. **触发器**：添加库存自动预警触发器、订单金额自动计算触发器
3. **行级权限**：会员只能查看自己的订单，可通过视图或应用层实现
4. **索引优化**：根据实际查询频率进一步优化索引策略
5. **数据备份**：添加定期备份和恢复机制

---

## 四、交付物清单

```
SQL-job/
├── README.md                    # 项目说明、业务角色、复现步骤
├── 第一阶段报告.md               # 本报告
├── .gitignore
├── sql/                         # 统一SQL脚本（9个脚本+README）
│   ├── 01_create_database.sql
│   ├── 02_create_tables.sql
│   ├── 03_insert_sample_data.sql
│   ├── 04_crud_operations.sql
│   ├── 05_index_optimization.sql
│   ├── 06_queries.sql
│   ├── 07_views.sql
│   ├── 08_constraints.sql
│   ├── 09_roles.sql
│   └── README.md
├── result/                      # 执行结果（10个文本+7张截图+README）
│   ├── 01-10_*.txt
│   ├── screenshots/
│   └── README.md
├── week1/ ~ week4/              # 各周交付物（文档+SQL+报告+AI记录+分工表）
```

**第一阶段 v0.1 数据库原型已完成，从空数据库开始可以完整复现，达到课程要求。**
